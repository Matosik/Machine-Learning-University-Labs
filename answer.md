## 1. Постановка задачи классификации
Что такое: Задача отнесения объектов к одному из заранее известных классов на основе их числовых характеристик.

Ключевые элементы:

Множество объектов: $Ω = {ω₀, ω₁, ..., ωᵢ₋₁}$

Классы: Ω₀, Ω₁, ..., Ωₗ₋₁ - непересекающиеся подмножества

Вектор признаков: x̄ = (x₀, x₁, ..., xₙ₋₁)ᵀ - числовое описание объекта

Пространство признаков: D - множество всех возможных векторов признаков

Суть задачи: Построить решающее правило (отображение), которое по вектору признаков x̄ указывает принадлежность объекта к одному из классов Ωₗ.

Пример: По весу и размеру фрукта определить, является ли он яблоком, апельсином или грушей.

## 2. Определение классификатора
Классификатор - это алгоритм (правило, устройство), который:

Принимает на вход вектор признаков x̄

Выдает решение о принадлежности к одному из классов Ωₗ

Математически: Классификатор задает разбиение пространства признаков D на непересекающиеся области $D₀, D₁, ..., Dₗ₋₁$, такие что:


если $x̄ ∈ D_l$, то решение: объект принадлежит классу Ω_l
## 3. Способы задания классификатора
Основные способы:

Через области пространства признаков:

Явное задание областей $D₀, D₁, ..., Dₗ₋₁$

Через дискриминантные (решающие) функции:

Набор функций $d₀(x̄), d₁(x̄), ..., dₗ₋₁(x̄)$

Правило: $x̄ ∈ D_l$ если $dₗ(x̄) ≥ dⱼ(x̄)$ для всех $j ≠ l$

Важное свойство: Дискриминантные функции определяются не единственным образом. Можно применять:

Линейные преобразования: $g₁(x̄)dₗ(x̄) + g₂(x̄)$

Монотонные преобразования: $φ(dₗ(x̄))$

## 4. Качество классификатора
Основные метрики:

Вероятности ошибок:

$pₗⱼ = P(x̄ ∈ Dⱼ | Ωₗ)$ - вероятность отнести объект класса Ωₗ к классу Ωⱼ

При l = j - правильная классификация, при l ≠ j - ошибка

Общий риск (средние потери):


$R = ∑∑ cₗⱼ · P(Ωₗ) · pₗⱼ$

где:
cₗⱼ - штраф за ошибку (из матрицы потерь)

P(Ωₗ) - априорная вероятность класса

pₗⱼ - вероятность ошибки

Чем меньше R, тем лучше классификатор.

## 5. Байесовский классификатор
Условия применения: Известны ВСЕ параметры:

Функции правдоподобия f(x̄/Ωₗ)

Матрица штрафов C = ‖cₗⱼ‖

Априорные вероятности P(Ωₗ)

Цель: Минимизировать общий риск R

Решающее правило (для простейшей матрицы штрафов):


$P(Ωₗ) · f(x̄/Ωₗ) ≥ P(Ωⱼ) · f(x̄/Ωⱼ) ⇒ x̄ ∈ Dₗ$
Дискриминантные функции:


$dₗ(x̄) = P(Ωₗ) · f(x̄/Ωₗ)$


## 6. Минимаксный классификатор
Условия применения: Известны всё, КРОМЕ априорных вероятностей P(Ωₗ)

Идея: Минимизировать максимально возможный риск (защита от наихудшего сценария)

Стратегия: Найти такой классификатор, для которого величина общего риска постоянна при любых априорных вероятностях

Для двух классов с простой матрицей потерь:


$p₀₁ · c₀ = p₁₀ · c₁$
(уравниваем взвешенные вероятности ошибок)

## 7. Классификатор Неймана-Пирсона
Условия применения: Известны ТОЛЬКО функции правдоподобия

Цель: Минимизировать одну ошибку при фиксированном значении другой

Математическая постановка:

$\begin{cases}
p₁ → \min \\
p₀ = p₀^*
\end{cases}$

где p₀ - вероятность ложной тревоги, 

p₁ - вероятность пропуска цели

Решающее правило:


$Λ(x̄) = \frac{f(x̄/Ω₁)}{f(x̄/Ω₀)} \begin{cases}
< λ ⇒ x̄ ∈ D₀ \\
≥ λ ⇒ x̄ ∈ D₁
\end{cases}$

Порог λ находится из условия $p₀ = p₀^*$

## Функция правдоподобия 
"Насколько вероятно увидеть именно такие признаки x при условии, что мы имеем дело с классом Ωl?"
Другими словами, если мы знаем наверняка, что перед нами кошка (Ωl), то какова вероятность (или плотность вероятности), что у неё будут именно такие параметры x (усы такой длины, уши такой формы и т.д.)?

## 8. Отношение правдоподобия
Определение:

$Λₗⱼ(x̄) = \frac{f(x̄/Ωₗ)}{f(x̄/Ωⱼ)}$

Смысл: Показывает, во сколько раз наблюдение x̄ более "похоже" на класс Ωₗ, чем на класс Ωⱼ

Применение: Все три оптимальных классификатора (Байеса, минимаксный, Неймана-Пирсона) являются классификаторами отношения правдоподобия - они отличаются только способом выбора порога.

## 9. Байесовский классификатор для нормально распределенных признаков
Общий вид плотности:


$f(x̄/Ωₗ) = \frac{1}{\sqrt{(2π)ⁿ|Bₗ|}} \exp\left(-\frac{1}{2}(x̄ - Mₗ)^T Bₗ^{-1}(x̄ - Mₗ)\right)$
Дискриминантные функции:

$dₗ(x̄) = \ln P(Ωₗ) - \frac{1}{2} \ln |Bₗ| - \frac{1}{2}(x̄ - Mₗ)^T Bₗ^{-1}(x̄ - Mₗ)$
Частные случаи:

Одинаковые диагональные матрицы (Bₗ = DₓI):

Классификатор по минимуму евклидова расстояния

Линейные разделяющие поверхности

Одинаковые корреляционные матрицы (Bₗ = B):

Классификатор по минимуму расстояния Махаланобиса

Линейные разделяющие поверхности

Различные корреляционные матрицы:

Квадратичный классификатор

Квадратичные разделяющие поверхности

## 10. Байесовский классификатор для распознавания бинарных признаков
Особенности: Признаки xᵢ ∈ {0, 1}

Дискриминантные функции (при независимости признаков):


$dₗ(x̄) = \ln P(Ωₗ) + \sum_{i=0}^{N-1} [xᵢ \ln pᵢₗ + (1-xᵢ) \ln(1-pᵢₗ)]$

где $pᵢₗ = P(Xᵢ = 1/Ωₗ)$

Важно!: Классификатор оказывается линейным относительно бинарных признаков.

## 11. Вычисление вероятностей ошибочной классификации
Общая формула:

$pₗⱼ = ∫_{Dⱼ} f(x̄/Ωₗ) dx̄$

Сложность: Вычисление многомерных интегралов

Упрощения:

Через отношение правдоподобия (для 2 классов):

$p₀ = ∫_{λ}^{+∞} f_Λ(u/Ω₀) du$ 

$p₁ = ∫_{-∞}^{λ} f_Λ(u/Ω₁) du$

Для нормального распределения:

Выражаются через функцию Лапласа

Зависят от расстояния Махаланобиса между классами

Для бинарных признаков:

При малой размерности: прямой перебор 2ᴺ комбинаций

При большой размерности: приближения через ЦПТ

Экспериментальная оценка:


$p̂ = \frac{\text{число ошибок}}{\text{общее число объектов}}$


## 12. Риски R(x) все формулы


## 13. Логарифм отношения правдоподобия зачем нужен ? 

1. Численная стабильность (самая важная практическая причина)
Правдоподобия f(x/Ω) часто являются очень маленькими числами (особенно в многомерных пространствах), что может приводить к проблемам:
Переполнение снизу (underflow): когда числа настолько малы, что компьютер не может их представить и округляет до 0
Потеря точности при работе с очень малыми числами

## 14. Ошибка 1 и 2 рода
Ошибка 1 рода когда мы допускаем ошибку простительную(обозначили здорового как больного или подумали что обычный человек террорист)
Ошибка 2 рода когда мы допускаем непрастительную ошибку (обозначили больного как здорового или пропустили террориста)
 